# Configuration des variables d'environnement
# Copiez ce fichier vers .env et ajustez les valeurs selon votre environnement

# ============================================
# CORS Configuration
# ============================================
# Liste des origines autorisées pour les requêtes CORS, séparées par des virgules.
# En développement, si cette variable n'est pas définie, seules les origines locales
# (http://localhost:5173 et http://127.0.0.1:5173) sont autorisées par défaut.
# 
# IMPORTANT: En staging/production, vous DEVEZ définir cette variable avec tous les
# domaines autorisés, sinon toutes les requêtes XHR depuis le frontend échoueront
# avec une erreur CORS.
#
# Format: liste d'URLs séparées par des virgules (espaces optionnels)
# Exemples:
#   - Pour un seul domaine: CORS_ORIGINS=https://app.mondomaine.org
#   - Pour plusieurs domaines: CORS_ORIGINS=https://app.mondomaine.org,https://admin.mondomaine.org
#   - Avec ports: CORS_ORIGINS=https://app.mondomaine.org,http://localhost:3000
#   - VPS sans domaine (écran blanc si absent): CORS_ORIGINS=http://IP_DU_VPS:3000
#
# Note: En développement (NODE_ENV != production), les origines non listées sont
# loggées avec un avertissement mais autorisées pour faciliter les tests locaux.
CORS_ORIGINS=https://app.mondomaine.org,https://admin.mondomaine.org

# ============================================
# Environnement
# ============================================
# Environnement d'exécution: development, staging, production
# En production, CORS est strictement appliqué selon CORS_ORIGINS
NODE_ENV=development

# ============================================
# API Configuration
# ============================================
# Message de réponse pour l'endpoint /api/ping
PING_MESSAGE=ping

# ============================================
# Stockage objet des documents (S3 / MinIO / Azure Blob)
# ============================================
# Bucket / conteneur principal où seront stockés les fichiers.
# - Pour S3: nom du bucket
# - Pour MinIO: nom du bucket existant
# - Pour Azure Blob: nom du conteneur (via passerelle S3 compatible)
OBJECT_STORAGE_BUCKET=mon-bucket-documents

# Région du bucket (S3 ou passerelle compatible)
OBJECT_STORAGE_REGION=eu-west-1

# Endpoint optionnel pour un stockage S3 compatible (MinIO, Scaleway, Cloudflare R2…)
# Laisser vide pour AWS S3 natif.
# Exemple MinIO local: http://localhost:9000
OBJECT_STORAGE_ENDPOINT=

# Forcer le path-style (utile pour MinIO / compatibility layers)
OBJECT_STORAGE_FORCE_PATH_STYLE=true

# Active un hook antivirus (à brancher sur un service externe).
# Pour le moment, le hook est un no-op et ne fait rien.
ANTIVIRUS_SCAN_ENABLED=false

# ============================================
# Builder.io (si utilisé)
# ============================================
# Clé publique Builder.io pour l'intégration frontend
# https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
